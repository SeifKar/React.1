import React from 'react';
import product from '../product';

const Price = () => {
    return (
        <div className="product-price">
            ${product.price}
        </div>
    );
};

export default Price;
